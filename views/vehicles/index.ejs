<%- include('../partials/header') %>

<h2>All Vehicles:</h2>
<table>
    <!-- <thead id="labels">
      <tr>
        <th>Year</th>
        <th>Make</th>
        <th>Model</th>
        <th>Color</th>
        <th>Services</th>
        <th>Driveable</th>
      </tr>
    </thead> -->
    <tbody class="container">
      <!-- Write the line of EJS to iterate over movies using forEach -->
      <% vehicles.forEach(function(v) { %>
        <div class="card">
        <tr>
            <td><%= v.year %></td>
            <td><%= v.make %></td>
            <td><%= v.model %></td>
            <td><%= v.color %></td>
            <td><%= v.services %></td>
            
            <!-- finish the ternary expression to display
                'Yes' or 'Nope' --> 
                <td><%= v.driveable ? 'Yes' : 'No'  %></td>
                <br>
                <td><a href="/vehicles/<%= v._id %>">Reports</a></td>
                <td>
                    <% if (user) { %>
                    <form action="/vehicles/<%= v._id %>?_method=DELETE"
                    class="delete-form" method="POST">
                    <button class="trash" type="submit">🗑️</button>
                  </form>
                  <% } %>
                </td>
               
                
                  
            </tr>
            
            </div>
      <!-- Close the forEach using EJS here -->
      <% }); %>
    </tbody>
  </table>
  

<%- include('../partials/footer') %>
